# v0.1.278-bh.027：每次交付记录双上游版本快照

本版只完善版本留档约束，并补齐 bh.026 的可读版本对照；不同步新代码、不构建镜像。

## 元数据

| 字段 | 内容 |
| --- | --- |
| 文档 ID / 日期 | v0.1.278-bh.027 / 2026-09-16 Asia/Taipei |
| 类型 / 交付状态 | 纯文档与约束；检查中，未构建/推送应用镜像、未部署 |
| 前序记录 | [bh.026](v0_1_278_bh_026.md) |
| fork 起点 / 实现定位 | 248a9b3ca；本记录提交由 Git 历史定位 |
| 应用版本 / 最近应用产物 | VERSION 0.1.278；镜像 0.1.278-bh.026，源码 987e757bbd43b2acd0e4c1cf85c789c69722078c |

### 双上游版本快照（沿用 bh.026，本次未同步）

| 项目 | 发布标签及实际发布提交 | 上次选取的目标 VERSION / SHA | 覆盖状态 |
| --- | --- | --- | --- |
| TokenFlux/TokenRouter | v0.1.278 → e7906995f5f722655dd5f593b88651d39abb16a8 | 0.1.278 / 0eba20d2ff7e2758e4e0f53589279f8b85c33a7a | 发布后的 main；bh.026 选择性适配，不是发布点或整段主线全量合并 |
| Wei-Shaw/sub2api | v0.2.5 → 86f93c28ee34cc74b629dafb748bd5ac5ca8c5ea | 0.2.5 / 881f3202694c6bc932446931a30c27d9675178b9 | 目标为发布提交的紧邻子提交，仅回写 VERSION；bh.026 只补图片、WS、额度等选定项 |

TokenRouter 目标的 sub2api 处理边界仍为 0.2.1 / `ab99d56e9626e6cd731592dae8553c9758a0efa2`，由 `5076636925b805a90c6834e6c02bebd25f115d72` 记录为已处理；它含跳过决定，不代表功能全部覆盖，也不是 ByteSeek 已补到 0.2.5 后改变了 TokenRouter 的处理边界。

## 需求与范围

用户要求每次改完都记录两个上游对应版本，并核对刚完成的应用批次。之前 bh.026 已有固定 SHA 和补丁选择表，但缺少一眼可读的 Release、VERSION 与目标提交区别；普通非同步交付的模板也未强制逐项沿用双源快照。

## 功能变动

| 变动 | 之前 | 之后 |
| --- | --- | --- |
| 每次留档 | 同步时要求固定基线，非同步可仅写“未变” | 每份交付记录都列 TokenRouter/sub2api 版本、SHA、覆盖方式；未同步明确沿用前版并链接来源 |
| 版本区分 | VERSION 与发布标签可能被混读 | 分别保存标签指向的 commit、实际取用 main/版本回写 commit、ByteSeek 应用与镜像版本 |
| 历史查询 | bh.026 主要展示 SHA | 追加可读版本对照，不改其原始实现与发布事实 |
| 当前索引 | “当前基线”段落仍停留在 bh.014 的回滚时点 | 区分历史回滚起点、后续演进、最近应用 bh.026 与本次纯文档修订 |

## 开发逻辑与依据

AGENTS.md 提供每次任务入口门禁；version_history.md 规定字段与验收；dual_upstream_sync_contract.md 解释 Release/目标 SHA/处理边界的区别；template.md 固定可复制的快照结构。历史索引只指向最新快照，具体证据放在版本记录，不建立一个会自动跟随远端漂移的“最新主线”标记。

来源证据使用只读 `git show <固定 SHA>:backend/cmd/server/VERSION`、`git ls-remote --tags` 与固定两提交 diff。sub2api v0.2.5 发布点和 881f320269 之间只有 VERSION 回写；TokenRouter 0eba20d2f 不是 v0.1.278 发布点。选择性补丁的下一轮去重仍按 bh.026 逐功能表，不能把目标 SHA 当成全量同步完成游标。

## 上游同步与保留差异

本版无新上游业务补丁；两个目标及覆盖方式完整沿用上表和 bh.026。B0 为 248a9b3ca，应用源码未变，T0/T1 和 S0/S1/S-target 的应用同步历史不改。对 bh.026 仅追加标注日期和原因的版本证据，不回写原历史决定。

## 包豪斯适配

已读取包豪斯契约；本版只改 Markdown，不改前端布局、主题、组件或任何业务状态，包豪斯约束继续有效。

## 接口数据与兼容性

不改 API、鉴权、计费、调度、依赖、数据表/迁移、应用 VERSION 或 Git tag。版本模板字段不构成应用对外字段。

## 验证记录

- 已只读核对三个固定目标 VERSION：TokenRouter 0.1.278、sub2api 0.2.5、TokenRouter 处理边界 sub2api 0.2.1。
- 已核对远端 annotated tag 的 peeled commit，并确认 sub2api 0.2.5 目标为发布点的下一次纯 VERSION 回写。
- 文档链接、表格字段与 diff 检查待完成；不重新运行应用测试，因为没有运行时代码变动。

## 发布部署与回退

本版不构建/推送镜像、不创建 0.1.278-bh.027 应用标签，不移动 latest/bauhaus；最近应用产物仍为 bh.026。没有容器或生产数据操作。回退本版只需另建记录撤销新增文档约束，不回退 bh.026 应用代码。

## 风险与后续同步保留点

每次记录必须同时列两个上游，即使仅改 UI 或仅同步一个来源；未动的一方沿用已核实快照，不能换成当时远端最新版本。Release 标签可移动，必须记录当次核验的完整 SHA；无法核实时标明未知，不猜版本。选择性覆盖继续逐补丁审查，不能因版本相同而跳过后续新提交。

## 追加证据与勘误

无。
