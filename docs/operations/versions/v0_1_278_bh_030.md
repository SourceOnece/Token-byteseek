# v0.1.278-bh.030：双上游剩余能力的兼容适配

## 元数据

| 字段 | 内容 |
| --- | --- |
| 文档 ID / 日期 | v0.1.278-bh.030 / 2026-09-16 Asia/Taipei |
| 类型 / 状态 | 双上游同步；代码与测试完成，未构建/推送镜像、未部署 |
| 前序记录 | [bh.029](v0_1_278_bh_029.md) |
| ByteSeek B0 / 应用版本 | `bf1a089bdb9985432b29fcf8308d0e0f08fd9821` / 0.1.278 |
| TokenRouter 发布 / 固定目标 | v0.1.278 → `e7906995f5f722655dd5f593b88651d39abb16a8`；T1=`0eba20d2ff7e2758e4e0f53589279f8b85c33a7a` |
| sub2api 发布 / 固定目标 | v0.2.5 → `86f93c28ee34cc74b629dafb748bd5ac5ca8c5ea`；S-target=`881f3202694c6bc932446931a30c27d9675178b9` |
| 上次比较边界 | T0=`1910b44ac5b766f4ce00e99f7b51d1c484cc3c71`；S0=`5097b31457e6dc9f49e5f5c9c72b925ce79543b3`；S1=`ab99d56e9626e6cd731592dae8553c9758a0efa2`，处理边界不代表全功能覆盖 |
| B1 / 实现定位 | 工作区已完成本批选定适配；未纳入项在下方保留，源码提交以本记录所在提交定位 |

## 需求与范围

用户同意将上一轮清单中的 TokenRouter 和 sub2api 剩余更新基于 ByteSeek 现有逻辑适配。新收费倍率、强制模型白名单、整站计费模式接入能力但不自动改变现有账号、价格、订阅或分组；必要时通过显式可选配置启用。保持包豪斯、邮箱共用搜索、质量检测/定时任务/调度联动、TLS 与会话隔离、Asia/Taipei、原生 Images 1.5/2/2.5；不恢复已撤回 Metadata 实验和 Bearer 生成 AT。

## 功能变动

按阶段逐项更新实际结果，不把下面计划写成已落地。

| 阶段 | 待适配范围 | 当前状态 |
| --- | --- | --- |
| TokenRouter | Opus 5/Bedrock、Claude 归因、Ultrafast、可选 max 计费倍率、支付宝补偿、侧栏折叠、准入错误码、WS 风控补齐 | 该阶段工作区的完整 Go 单元测试已通过；前端类型和 94 项定向测试通过，后续新增内容须再回归 |
| sub2api 协议/账号 | Astra pro/指令、allowed_tools、agent_message、模型目录/上下文、Antigravity 隔离/工具/流、Ollama、图片内存 | Astra、Antigravity、Ollama 429/真实端点输出限制、图片内存与合成序号已适配；完整 pinned manifest 代理按上游撤回与本地号池边界不采纳 |
| sub2api 管理/运营 | 缓存广播、代理部分编辑、订阅/Key 批量操作、认证容错、备份与迁移互斥、监控、用户界面修复 | 缓存、代理、认证、备份锁、Key 批量编辑及订阅批量动作/分配已适配；独立 channel-monitor 模块在 ByteSeek 不存在，相关提交不恢复 |
| 新平台/准入/计费模式 | OpenCode Go/MiniMax、可选强制白名单、整站模式 | 两平台、白名单与销售模式已适配并做最终回归，不更改旧默认 |

## 开发逻辑与依据

先从 T1 最终有效补丁链逐项适配；新能力沿用本地领域 service、当前公共 UI 组件。T1 引入已有 sub2api 行为后重新去重，剩余补丁才补入。旧展示列表不能在迁移时变强制准入；新增 max 费用缺省不加收，原价格覆盖优先级和结算幂等保持。接口字段只能兼容扩展，不删除既有字段/路径；迁移按本地 270 后顺序编号。

## 上游同步与保留差异

### TokenRouter 选择

| 来源 | 本地起点 | 决定 |
| --- | --- | --- |
| `54b808b1c` + `0eba20d2f` | Sonnet 5 已有，Opus 5/区域推理 ID 差额缺失 | 适配完整有效链 |
| `da9c8d740` | 已有版本覆盖但计费后缀及最终 UA 对齐不完整 | 补差，不恢复 Codex Metadata |
| `c05ceab15` | Fast/Flex 已有 | 加 Ultrafast 显式档位，旧请求行为不变 |
| `f81f65255` + `082f9c752` | 无 max 独立计费倍率 | 加可选倍率，缺省 1；不自动引入 Fable 3 倍 |
| `8fb56e3df` | 微信待支付补偿已有 | 支付宝复用原补单状态机 |
| `0bcd9abd4` | 当前栏目强制展开 | 手动折叠优先，保留包豪斯 |
| `9630faee1` | 本地准入错误类型不细分 | 扩充 code，保持原状态码/消息 |
| `311cd8965` + `20e45d6f5` | 风控部分 WS 路径未记 | 补前置记录，保留本地告警/计费 |

### sub2api 剩余差异

按上一轮只读清单为候选，T1 实施后逐功能重新核对。已有 bh.026/bh.029 图片、断线排水、beta、GLM、Gemini 观测等不重复；上游撤回/无关文档与生成物不算待同步功能。未纳入项在本记录中明确原因，不以合并标记代替行为证据。

### 已适配的 sub2api 补差（未发布）

| 来源提交 | 本地实现及保留点 |
| --- | --- |
| `5485f368b`、`8193b80a5` | Astra 保留 `reasoning.mode=pro` 与 effort，不把 mode 降为 effort；空提示词按最终映射模型选择，保留已有提示词、5.6 Sol/Terra/Luna 别名及本地 TLS 转发入口 |
| `43491f67a`、`310f8b7fa` | 保留 allowed_tools 引用及子智能体 agent_message 文本，兼容现有 Responses→Chat 转换 |
| `1e1d15cca`、`b8275209e`、`f7c48ba59`、`150c52b8d` | Antigravity token 按账号 ID 隔离并清旧 project 键；流式分隔空行不当数据；空工具仍有 toolConfig；混用工具优先保留客户端函数、移除不兼容内置搜索/代码执行（明确行为变化） |
| `6254aba5e`、`1f716aa18`、`dd2e6b368`、`979fe247f` | 补 3.7/3.8 Flash 预设/兜底，默认 UA 更新，刷新警告返回实际账号，计划字段可以随授权保存；自定义版本和实际账号配置优先 |
| `ba57ea914` | JWT/Admin 数据库临时故障返回 500 而非 401；浏览器刷新网络/429/5xx 错误不清登录，保留本地跨标签页刷新协调和换号保护 |
| `15557e779` | 渠道缓存 Pub/Sub 接入本地 Wire/关闭流程；另补代数保护成功/失败旧查询，防陈旧价格回写及广播反馈循环 |
| `2061508ad` | 双粘性命中率按请求一次计数，保留本地高级调度延迟与策略 |
| `9e7039ebc`、`f7e959ae5` | 代理更新区分省略/null/空串，新增 NullableInt64Field；只改状态/名称不会清有效期、备用和凭据，显式空串可清凭据；导入调用同步适配 |
| `c54897a59`、`28b807a73` | 代理备用边改为有向多对一并重新生成 Ent；再次过期继续回退但保留首个 origin；保留 Ollama 快照失效/outbox 与废弃探测字段清理 |
| `4892b8f17`、`5cbb9191a`、`329641a86`、`99b93b298` | 代理/公告日期 JSON 范围校验，代理坏响应拒绝，部分导入成功关闭后刷新，筛选回第一页；保留本地收纳式筛选布局 |
| `88011a6a2`、`25c21aedd` | 大图片读取/元数据改写减少重复复制；合成 Responses SSE/WS 事件写序号；不改透传事件序号、请求上限、图片内容或断线结算 |
| `a59f0c7fa` | Fast 策略新增 missing 显式条件，旧 all 不匹配缺省字段；本地统一决策器复用 HTTP/WS，仍受 Key 强制关闭及全局拦截约束 |
| `f0dd49778`、`67845665d`、`0a378f343` | CSV 导出固定查询条件快照，通知邮件按身份删除避免并发索引错位，Key 重置只更新对应行 |
| `a444551a6`、`badfad8b7`、`ae4cc14b2`、`909b8c7ef`、`4ff3e6dfb`、`f2b51e3b2` | 注册确认密码（不新增 API 字段）、空优惠码既有行为补测、注册入口按开关显示、验证码加载提示、兑换成功后刷新失败独立警告、资料修改结构化错误提示 |
| `fd300ab6f`、`3eb5a84ea`、`2b1e5e7f5` | DeepSeek 新 Flash 价卡与 Pro 切换时刻，账号默认成本共用请求计费时刻；GLM-5.3/Flash 独立兜底价。分组/渠道显式价、Qoder 路由价规则与已记录历史账单不覆盖 |
| `b9ebc8f20` | 本地已有更完整同版本 Pro/Flash 档位解析，复用，不替换为上游枚举版本的实现 |
| `dfa83fbe9` | 新增站点购买方式配置与公开首屏投影，默认旧行为；只影响新购买入口，保留已购订阅、顶栏进度、历史筛选、管理员管理，不采用上游整面隐藏。沿用原支付总开关、余额充值禁用和订单恢复流程 |
| `f8351e9e3` | 独立新增 `model_allowlist`，不采用上游把旧展示列表重命名并激活为准入的迁移。HTTP 在本地复合 Key 选组后、Key 重定向前检查；WS 使用映射前路由 hook，避免原生 ingress 已改写 U 的正文误拒绝别名。追加兜底分组检查、普通/复合 Key 持久失效与快照深拷贝；菜单保持独立 Toggle/模型列表控件 |

上述提交已按本批选择适配进工作区；完整 pinned manifest 代理、上游独立 channel-monitor 和已撤回功能明确不采纳。站点模式、白名单、模型发现、批量运营等回归通过；真实外部模型/支付和生产负载仍不在测试覆盖内。

### 后续阶段已落地（工作区，未发布）

| 来源 | 实现、兼容与证据 |
| --- | --- |
| `88b697d5d` | `backup_pg_dumper.go` 在 dump 流生命周期持有迁移锁，异常连接丢弃；保留本地 BackupDumpOptions 表数据排除。Wire 重新生成，定向测试通过 |
| `9fade3e8c` | Ollama 429 先不缩短冷却，再使用有界合并探测；新结果以身份和限流代次 CAS 写库，调度通知跟随权威状态。真实 PostgreSQL 验证旧代次、清除和新代次不会互相覆盖 |
| `14e0a49e1` | 仅实际 HTTPS Ollama 官方目标适配 DeepSeek 输出上限、Messages Bearer；Chat/Responses/Messages 复用各自原转发，保留其它模型的旧强制 Chat 策略、配置关闭行为与 TLS 入口 |
| `0bba06d64` | Key 当前页多选及显式字段勾选弹窗，复用原 PUT 权限，分批并发最多 5；部分失败只重试失败项。复合 Key 禁止批量改单分组，仍可改其余配置。47 项定向测试及类型检查通过 |
| `1920a7d68` | 新 `/admin/subscriptions/bulk-action` 支持增减天数、指定窗口重置、撤销、恢复；每条独立事务先锁用户再读套餐链，保留 pending 与恢复冲突语义，不覆盖单条设置目标天数的老接口。强制幂等键，客户端断线后两分钟有界执行、五秒保存结果；初始租约覆盖整个时限，不移植无必要的续租协程。补入多用户批量分配 UI，默认仍为单用户；复用旧 bulk-assign 路径，仅带键的新调用走严格协调器，无键旧调用兼容。用户去重、结果不明固定参数/键、部分失败仅保留失败用户；真实 PostgreSQL 重放和续期链验证通过 |
| `0be30886a`、`d2067668d`、`8f7f2b5d9` | 订阅分配只搜索当前用户，文字改动即清旧目标并隔离迟到查询；历史筛选仍可看已删除用户；邮箱蓝色链接到对应使用记录。10 项页面回归通过 |
| fork 并发修复 | race 检测实测发现 WS 上下行共享会话模型字符串竞争，改为原子指针读取/更新，不变更路由和响应模型恢复语义；新并发回归及相关 race 通过 |
| fork 白名单补边界 | 创作台 JWT 目录/创建路径也按公开模型名过滤；执行前复核硬规则，拒绝时走原失败释放预占。批量图片在上传/预占前补服务层检查，Key 别名语义不变；关闭开关保持原逻辑，相关图片套件通过 |
| `ce328fb37` | 支付和管理员可信发放隔离公共兑换失败限流，保留本地码行锁、次数/用户记录、套餐与事务；订单兑换码查询故障不再当作不存在，匹配金额/用户/单次码并在锁内复核。公共兑换仍限流，管理员返利与订单返利保持各自去重。支付/兑换/管理端及提供商回归通过 |
| `e67ffda7a` | 易支付仅上游 type 放宽点号，前后端验证及实际提交参数测试覆盖；本地类型格式不放宽，支付弹窗使用原组件 |
| `9c30951e4` | 续费套餐弹窗增加动态视口最大高度和内部滚动，标题/关闭按钮不被长列表挤出；同步改为现有包豪斯硬边/硬阴影，未改支付选择行为 |
| `fa556700c` | 本地已有净化 Markdown 支付帮助文案及 XSS 回归，不重复迁移上游类名与样式；既有测试通过 |
| `67d3a896b` | 本地兑换订阅选择套餐而非输入分组有效期，不存在上游 365 天表单上限；不恢复旧字段，保留套餐有效期作为权威（有意不照搬） |
| `dbe92a1c2` | MiniMax 平台适配现有十平台矩阵、三类入站及四种上游协议、创建/编辑/模型同步、Key 客户端配置、额度/监控/调度快照及错误平台。Coding 用量接本地统一只读适配器，配置主机保留，拒绝不完整/非法百分比，不照抄跨主机字符串判断；payg 不伪造余额。MiniMax 红色重点、既有品牌图标与移动两列布局。旧账号默认不动，修正旧保存校验与界面已支持的 Kimi Responses/adaptive 不一致 |
| `3ee92b40c` / 原 `941a0487a` | 编辑账号合并活跃分组列表和详情中的已绑定停用分组，保留活跃列表最新投影、排除未绑定停用项；公共 GroupSelector 接受精简分组，不补造管理字段，保留旧平台过滤 |
| `2d9af8592` / 原 `55a95d4c6` | 创建普通 Key 增加四类供应商筛选，真实平台分类并保留 Qoder；先套用本地个人/团队/指定订阅交集。编辑、复合 Key、列表筛选和 API 不变；复用品牌图标、单选、硬边/硬阴影/按压，不引入上游大圆角卡片 |
| `799e9938f`、`1c0932e17`、`642d20b8a`、`f88d62ad2` | 模型发现按最终可见目录增加单项 `GET /v1/models/{model}`/裸别名，保留本地复合前缀和路径模型名；管理员 MiniMax/OpenCode 选择器使用自身预设，显式映射优先。不上游完整 Codex manifest 代理（其后续已撤回，且会越过本地账号池/缓存边界） |
| `abf70750f` | 公共兑换失败计数改为 Redis Lua 固定十分钟窗口，新版本键与旧 24 小时键隔离；成功、支付履约、管理员可信发放不增加公共失败计数 |

MiniMax 迁移原 237→本地 273，仅扩展真实存在的额度 CHECK，不恢复已删除的 Composite/监控 schema。新用户十平台初始化验证通过，既有额度不改写；内置模型列表只提供本批上游预设，价格/模态/上下文无证据不补造。后台协议/额度/模型解析定向回归、前端创建编辑/预设/平台 94 项与 Key 配置导出 24 项通过；代理/TLS 复用既有实现，未使用真实 MiniMax Key 调用官方。

本阶段事务/幂等真实测试覆盖重复批次回放、重复 ID、部分成功、写到期日后故障整体回滚、后续 pending 时间链、指定窗口归零与撤销恢复。现行订阅从数据库读取资格，不照搬上游已移除的按分组缓存。批次不是持久任务系统：进程崩溃或最终结果写入失败时不得换新键盲目重跑，应核对现状，版本不作跨崩溃 exactly-once 承诺。

## 包豪斯适配

继续去重与落地：`c7ed61424` 的全平台 Token 请求统计已经由本地 `ops_repo_token_stats.go/GetTokenStats` 实现，不恢复上游 OpenAI 专用命名。`0aac71c6e` 为错误详情显式启用摘要优先列排序，时间/错误内容排前但其他表格顺序/列设置不变；`8efe2fd8c` 费用明细改 8 位，通过本地币种格式化函数保留余额单位，不改实扣精度。`324a47e2b` 用户表格复用原删除 API 增加确认后批量删除，按确认 ID 快照串行处理，失败保留勾选，管理员保护继续由后端执行；合成回归未删除任何真实用户。以上定向前端 43 项通过。

`3a070ec1b` 的隐私/账号信息检查客户端采用 Firefox 配置，只有原 `Impersonate=true` 工厂路径改变，普通 OAuth refresh req client、HTTPUpstream/TLS 模板和 Codex Metadata 不变。增加 cf-mitigated 识别与警告，新增警告不包含响应正文；失败不写隐私成功或猜测订阅信息。工厂隔离及隐私/账号查询定向 Go 回归通过，未在真实 Cloudflare/账号环境检验，不承诺消除所有挑战。

所有新增配置、批量弹窗及新平台接入现有 Toggle/Select/对话框和主题变量；保留硬阴影、直角、红黄蓝与状态重点色。交付前做桌面/手机、深浅主题、中英、按压/焦点/禁用及减少动画检查，未测项另列。

## 接口数据与兼容性

开发中。新增渠道 max 倍率列迁移为 `271_channel_max_reasoning_effort_multiplier.sql`（上游 268→本地 271），NULL 表示不加价，历史数据不填 3 倍。代理关系修复仅修正 Ent 关系方向，现有 SQL 外键无新增列。代理字段支持显式清空属于兼容扩展，不应原样用旧后端处理新清空行为。DeepSeek/GLM 内置默认价格有来源快照带来的业务变更，必须与包豪斯视觉变更分开告知；已购订阅、账号状态、调度门禁及历史账单不回写。

分组白名单迁移为 `272_group_model_allowlist.sql`（上游 235 的独立兼容适配，不执行原重命名），旧展示列完整保留，新列缺省关闭。与上游 236 修复列缺失的目的相同，本地以新编号直接 ADD IF NOT EXISTS，不能复用已有历史编号。迁移同时扩展 group auth outbox 为该字段变化通知普通/复合 Key；Ent 已重新生成。回退旧二进制可以忽略新增列，但会失去硬白名单执法，因此启用规则后不可把旧实例留在服务链路中。

## 验证记录

本批最终门禁已完成，发布前仍需执行固定镜像构建与 GHCR 清单核验。真实官方额度/外部支付与生产负载不在合成测试覆盖内。

2026-09-17 续验：包含 OpenCode 的完整后端 unit 已通过（约 236 秒）。真实临时 PostgreSQL/Redis 的十一平台额度、订阅批量动作和新增批量分配重放/部分失败/排队链全部通过；新增批量分配前端 36 项和类型检查通过。停用绑定分组移除、供应商筛选/套餐范围/复合兼容及既有账号和 Key 页面合计 105 项通过；幂等响应显式大小上限新增回归通过。

同日补充：模型单项发现/复合密钥路径、MiniMax/OpenCode 管理模型目录和兑换固定窗口定向 Go 测试通过；完整 Go unit 344 个包通过，前端 344 个测试文件/2487 项通过，前端构建通过；模型发现、订阅批量和 OpenCode/MiniMax 浏览器场景在桌面/手机、深浅主题、中文/英文及减少动画模式通过。

阶段证据（开发中）：TokenRouter 范围 `Bedrock|BillingHeader|ReconcilePending|MaxReasoning|ReasoningEffort|Ultrafast|OpenAIFast|ServiceTier|ConcurrencyError|CyberPolicy` 的 service/handler/repository/admin/apicompat/migrations Go 测试通过；首次发现设置测试桩不支持本地 TTFT 批量读取，补合成读取后重跑通过。前端 typecheck 和侧栏/付款/模型预设/推理设置/层级文案 8 文件 94 项通过。最大推理倍率迁移编号 268→271，NULL 不加价；显式倍率由模型广场提示，与标准/Fast 基础价格区分。

后续阶段证据：TokenRouter 阶段完整 Go unit 在 `/tmp/bh030-t-full.log` 通过。sub2api 阶段修正上游测试未覆盖本地 DoWithTLS、余额展示 mock、代理有向边 SQL 预期及关闭流程参数。代理前端 40 项、登录注册/个人资料/验证码/兑换 35 项、API 刷新容错 24 项通过；`Deepseek|GLM53|AccountStats|LargeNative|ChannelCache|Astra` 定向 Go 回归通过。前端 typecheck 已通过至此阶段。完整后端再次运行中，不把此前只有业务包通过、cmd/server 测试参数缺失的失败轮次写成全量通过。集成数据库、race、全站浏览器及镜像验证尚未运行。

进一步验证：含独立白名单/销售模式的工作区完整 Go unit 已通过（`go test -tags=unit -p 2 ./... -count=1`，约 314 秒）。完整前端运行 335 文件/2393 项，2391 通过、两项旧断言仍认为 Anthropic 没有推理配置；仅更新预期后该文件全部 34 项重跑通过。真实临时 PostgreSQL/Redis 已完成全部新迁移，独立白名单默认关闭、展示列保留、普通/复合 Key 投影与 outbox、共享有向备用代理、重复过期回退及恢复测试通过。集成编译中修正一处遗留 `Wei-Shaw/sub2api` 测试导入为当前模块路径，不改业务。race、完整 WS 黑盒、包豪斯截图与镜像仍待完成；剩余功能实施后需重跑最终门禁。

后续实际验证：完整 Go unit 再次通过（包含备份/Ollama，尚未包含之后的订阅批量动作）；完整前端 335 文件/2394 项全部通过（尚未包含之后的批量 UI）。新订阅/幂等定向 Go、创作台/批量图片定向 Go 通过；前端对话框/操作键/公共关闭按钮与 Key 弹窗 41 项、订阅页面 10 项通过。真实 PostgreSQL/Redis 的订阅批次和账号限流 CAS 回归通过。`-race` 的白名单/渠道缓存/Ollama/Astra/代理/备份/WS 会话模型测试通过；本段是中途阶段证据，最终结果以本节顶部及下方补充为准。

最终门禁命令证据：Docker Go unit `go test -tags=unit -p 2 ./... -count=1` 全部包通过（约 323 秒）；前端 `pnpm test:run` 为 344 个测试文件、2487 项通过，`pnpm build` 通过；race 定向套件（ModelAllowlist/ChannelCache/Ollama/Astra/Proxy/PgDumper/OpenAIWSSessionModel）四个包全部通过（约 170 秒）。此前真实临时 PostgreSQL/Redis 迁移至 274、订阅批量分配和 MiniMax/OpenCode 额度验证均通过。

## 发布部署与回退

MiniMax 阶段最终完整后端 unit 已通过（约 226 秒），前端 342 文件/2454 项通过；后端前一轮仅设置 API 快照缺 MiniMax 默认额度字段失败，更新兼容新增字段预期后重跑通过。MiniMax 添加表单另在 1440×1000 与 390×844、中文浅色/英文深色执行真实浏览器交互及合成提交，平台保持 minimax、类型 apikey、自定义 Responses 地址保留，图标非空、直角/硬阴影/无横向溢出通过。后续隐私客户端与运营细节仍需纳入最终全量。

中断恢复后已完成一轮完整 Go unit（约 254 秒）和前端 342 文件/2448 项，均通过，包含支付/订阅批量/创作台白名单；这轮不含随后 MiniMax，新平台增加后仍须重跑最终全量。浏览器截图已等待真实 opacity=1 再检查，普通动画和减少动画均正常，并非透明弹窗样式问题。最新构建已包含续费滚动和 MiniMax，浏览器验证仍在继续。

本轮补充验证：支付/兑换/易支付/管理端 Go 定向套件及支付前端 39 项通过；修正旧单元夹具为实际订单单次码，不放宽生产校验。前端构建通过（已有大 chunk 警告）。本机合成数据 Playwright 对订阅批量确认/部分失败、Key 显式勾选编辑，在 1440×1000、390×844、中文浅色/英文深色及普通/减少动画组合检查通过；无页面级横向溢出，弹窗直角、4px 硬阴影、焦点、按压、禁用行为正确。仅访问回环预览，外部头像等网络资源被测试拦截，未用真实账号/支付；不代表全站/全部新配置已完成视觉门禁。续费滚动改动晚于这轮浏览器构建，需另测。

当前仍未构建/推送/部署。测试门禁已完成，下一步才可按规则构建固定 `0.1.278-bh.030`，再同步 latest/bauhaus，核对清单 digest；不替换线上容器。上一镜像 bh.029 清单 `sha256:57e86e81d551e38212024b58accaf6d867624df8144dfacfac3849407181494a`。涉及迁移后需逐项确认旧版本兼容，不能删迁移记录回退。

## 风险与后续同步保留点

OpenCode Zen/Go (`cdb5cfaf6`) 已适配：两种模式默认端点、三入口按映射后模型分派、单模型连接测试、统一只读 Go 用量和 CAS 监控、平台额度/调度/错误目录、管理创建/编辑规则均已接入。保留本地实际目标 URL 记录，不复制上游固定 endpoint 打标；模型名/价格/缓存沿用原转换语义。Go/Zen 的规则差异、固定协议优先、显式空数组含义均测试。新平台会话 ID 按本平台 Key、账号、上游凭据、客户端会话隔离，缺标识时仅同一请求重试复用随机值，不把普通 metadata.user_id 当会话；旧平台仍只向官方 OpenCode 主机原样转发显式会话头。原迁移 238→274，仅扩展本地额度表。

OpenCode 阶段九种跨协议组合、六种账号测试模式、会话隔离/重试和用量/凭据校验定向 Go 通过；前端创建编辑/规则 95 项、完整前端 343 文件/2472 项及构建通过。真实临时 PostgreSQL 完成到 274 的全部迁移，十一平台初始化与旧额度/唯一性、订阅事务/幂等及限流 CAS 验证通过。浏览器在四组中英/深浅/桌面手机视口完成添加 OpenCode、规则区自研 Select 检查和合成保存，无横向溢出，平台/自定义端点保持。完整后端轮次中业务包通过、设置 API 预期缺 OpenCode 空额度行；已更新明确快照并单独重跑 API 契约通过，还需整批最终完整重跑。没有真实 OpenCode/MiniMax 上游调用与生产验证。

上游两套业务并非原样可互换。需要保留用户已确认的旧默认：无新增默认加价、无自动强制白名单、无自动隐藏/关闭订阅；已有模型和账户分组授权优先。质量检测判定不是官方性能保证。不能承诺零风险，不能在清单未完成时发布或声称全量同步。

## 追加证据与勘误

无；实施中追加实际覆盖与测试，完成后记录源码与镜像证据。
