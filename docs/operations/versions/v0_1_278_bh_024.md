# v0.1.278-bh.024：撤回未发布的 Bearer 生成 AT 功能

## 章节导航

- [元数据](#元数据)
- [需求与范围](#需求与范围)
- [功能变动](#功能变动)
- [开发逻辑与依据](#开发逻辑与依据)
- [上游同步与保留差异](#上游同步与保留差异)
- [包豪斯适配](#包豪斯适配)
- [接口数据与兼容性](#接口数据与兼容性)
- [验证记录](#验证记录)
- [发布部署与回退](#发布部署与回退)
- [风险与后续同步保留点](#风险与后续同步保留点)
- [追加证据与勘误](#追加证据与勘误)

## 元数据

| 字段 | 内容 |
| --- | --- |
| 文档 ID / 日期（含时区） | v0.1.278-bh.024 / 2026-09-15 Asia/Taipei |
| 类型 / 交付状态 | 撤回完成，应用差异与原授权回归/前端构建通过；不发布、不部署 |
| 前序记录 | [bh.023](v0_1_278_bh_023.md)，未发布的功能尝试及实测记录 |
| 应用版本 / upstream SHA | 0.1.278 / 1910b44ac，不变 |
| fork 起点 / 实现提交定位 | main，4087e9c796ea0c9aac5dfe1b8a4b2272aaa56766；此次只撤回未提交变动，不重写历史 |

## 需求与范围

用户明确取消新增的 ChatGPT Bearer + 空间 ID 生成 AT 并入池功能。撤回 bh.023 的单条/批量新路由、服务、前端入口、类型、文案和专属测试，不再调用 Wham 或继续用真实凭据测试。保留现有 PAT 加号、OAuth/RT/auth.json 导入、账号邮箱列/共用搜索、质量检测、Asia/Taipei 与包豪斯风格。

## 功能变动

| 来源与功能 | 撤回前 | 撤回后 | 入口 |
| --- | --- | --- | --- |
| 未发布的 bh.023 生成方法 | 本地存在新 Bearer 单条/批量生成 AT 实现 | 删除新增路由/服务/控件，不保留隐藏开关 | OpenAI 授权方式和管理员 API |
| 旧 PAT/账号邮箱 | 既有功能未改 | 保持 bh.022 已发布基线行为 | 账号管理 |
| 版本历史 | bh.023 记录待验证尝试 | 保留原事实并追加撤回说明，本记录为当前决定 | 版本文档 |

## 开发逻辑与依据

先检查工作区与 HEAD 的逐文件差异，确认目标文件仅包含本次开发；对已跟踪文件反向应用本次 diff，对新功能专用文件精确删除。不使用整仓 reset、切分支、修改数据库或运行凭据接口。删除前在本机临时目录保留代码备份以便恢复，不写入任何真实令牌或客户信息。

| 文件/符号 | 处置 |
| --- | --- |
| backend/internal/service/openai_codex_at_service.go 及测试 | 移除未提交的新生成服务与测试 |
| backend/internal/handler/admin/openai_codex_at_handler.go 及测试 | 移除新 handler；原 OpenAIOAuthHandler 依赖字段与 routes/admin.go 新路由恢复 |
| frontend/src/components/account/CodexBearerImportPanel.vue 与 utils/codexBearerImport.ts 及测试 | 删除新增面板、行解析与专属测试 |
| CreateAccountModal / OAuthAuthorizationFlow / useAccountOAuth / accounts API / types / 中英文文案 | 只反向恢复 bh.023 的改动，旧 PAT、现有默认值及样式保留 |
| docs/interfaces/http_api.md、openai_upstream.md | 移除已取消功能的当前契约，历史开发理由保留在 bh.023 |

## 上游同步与保留差异

本版不 fetch/merge，不创建应用 tag，不修改应用 VERSION。目标是应用源码与撤回前原 HEAD 完全一致，只有版本留档差异。以后同步不可按 bh.023 历史描述重新引入已取消的功能。

## 包豪斯适配

移除未发布的新面板并恢复原授权区域，不改变全局样式、硬阴影、三原色、三元素、按压反馈或其他页面。原账号邮箱列及邮箱搜索属于已有功能，不在撤回范围。视觉/构建核验结果在下节记录。

## 接口数据与兼容性

不再提供本次新增的 create-from-chatgpt-bearer 及 /batch 路由；原 create-from-codex-pat 和其他鉴权/业务接口不变。没有数据库迁移、账号新增/删除、余额、计费或调度状态修改；实测曾返回手机号验证错误，未拿到 AT、未入池。

## 验证记录

| 检查 | 结果 / 说明 |
| --- | --- |
| `git diff --exit-code HEAD -- backend frontend docs/interfaces` | 通过；已跟踪应用代码及当前接口文档与 bh.022 后的原 HEAD 完全相同 |
| 新增文件及符号检索 | 生成服务、新 handler、新面板、解析器和专属测试已删除；backend 与 frontend/src 无新方法符号残留 |
| 原授权前端回归 | OAuthAuthorizationFlow 3 项、CreateAccountModal 24 项，共 27 项全部通过，包括旧 PAT 方法 |
| 前端构建 | `pnpm --dir frontend build` 通过（含 vue-tsc），保留既有大 chunk/动态 import 警告；内嵌 assets 检索无新方法入口/文案残留。仅重建本机资源，不是 Docker 镜像发布 |
| 原接口/邮箱保留 | create-from-codex-pat 仍注册原 handler，账号邮箱列及名称/邮箱共用搜索仍存在 |
| 版本链接 / `git diff --check` | 通过；bh.023 被删除代码的链接改为历史路径文字，原事实保留 |
| 后端运行与视觉 | 后端源码与已发布基线完全一致，本次不重新发起账号/上游集成测试；没有新样式改动，不宣称重新完整浏览器验收 |

## 发布部署与回退

bh.023 从未发布或上线，本次不需要替换镜像/容器，不创建 bh.024 应用镜像标签。GHCR 固定版 bh.022、latest、bauhaus 保持原状；本机前端资源须重新构建以移除旧实验资源，但这不是 Docker 发布。新旧版本记录与索引保留，恢复此实验需用户重新明确授权。

## 风险与后续同步保留点

本次仅证明取消新增功能，不将单次账号实测结果推断为该上游接口对所有账号无效。不得继续使用聊天中提交的令牌；不得根据历史记录自动恢复此实验。没有线上数据需要回滚。

## 追加证据与勘误

2026-09-15 Asia/Taipei：撤回后仅保留 bh.023 历史记录、bh.024 撤回记录与版本索引差异，应用源码与原 HEAD 完全一致。删除的本次未提交实验代码在本机临时归档 `/tmp/byteseek-bh023-withdrawal.RWjKrv/implementation.tar.gz` 可恢复（临时文件可能被系统清理，不是发布备份）；不含用户实测凭据，不进入 Git 或应用镜像。未操作生产数据、容器或 GHCR 标签。
