# v0.1.278-bh.059：新增账号票据工作台仅在第一步显示

## 章节导航

- [需求与范围](#需求与范围)、[功能变动](#功能变动)、[开发逻辑与依据](#开发逻辑与依据)
- [验证记录](#验证记录)、[发布部署与回退](#发布部署与回退)

## 元数据

- 日期：2026-09-20 Asia/Taipei；类型：新增账号界面修复；实现验证完成，GHCR三个同digest标签已发布，未部署。
- fork起点`cc56b92d4`，前序[bh.058](v0_1_278_bh_058.md)；不修改应用VERSION或创建Git tag。
- 上游基线保持TokenRouter v0.1.278 `0eba20d2ff7e2758e4e0f53589279f8b85c33a7a`、sub2api v0.2.7 `aea725f2ea644d5592d0bbb1d63b607efa7e200a`（历史核对main `1a9d49e16f7a22c432b428fce4af8d731f1fa364`）；本轮无上游检查或合并。

## 需求与范围

用户指出新增账号第一步“授权方式”已经设置票据，第二步“OpenAI账户授权”不应再次显示工作台。本轮只收口显示/草稿生命周期，保留默认模板和最终创建提交，不改后端、授权API、采集/调度判定。

## 功能变动

| 变更前 | 变更后 |
| --- | --- |
| 工作台位于步骤公共区域，授权步骤仍显示 | 只在第一步可见，第二步继续保留同一草稿实例 |
| 配置校验主要等最终创建 | 第一阶段进入授权前校验，错误时留在可编辑步骤 |
| 手机授权步骤的长地址撑出flex内容列，表单被裁切 | 内容列增加min-w-0及必要换行，保持原布局与授权行为 |

## 开发逻辑与依据

CreateAccountModal中工作台原v-if仅检查show和OpenAI OAuth资格，不看step，因此两个步骤都可见。保留该挂载条件，追加v-show=step===1，避免用v-if按步骤卸载后丢失草稿、重读模板或最终ticketCreationPatch拿不到ref。前进前沿原ensureReady/patch检查当前草稿；返回第一步继续同一实例，最终各创建入口仍通过ticketCreationPatch构造原codex_ticket字段，不增加第二个配置源。

关键文件：`frontend/src/components/account/CreateAccountModal.vue`、现有创建弹窗测试；接口与操作手册同步说明步骤归属。截图人工复核发现原OAuthAuthorizationFlow内容列在手机上被长地址撑宽（页面自身scrollWidth检查不能发现弹窗内部裁切），给外层及三步内容列增加min-w-0、外层break-words，补弹窗内部边界检查；不增加新的布局体系或修改授权逻辑。

## 上游同步与保留差异

无同步；保留ByteSeek账号工作台、导入默认值、特殊账号适用性、邮箱、批量配置和原接口。

## 包豪斯适配

仅修正现有工作台的步骤可见性，复用原硬阴影、网格及按钮，授权页保持原包豪斯；1440/390宽×深浅四组浏览器检查通过，深色使用减少动画偏好，无横向溢出。前进等待校验时沿原按钮禁用/加载状态，隐藏工作台不参与Tab焦点。

## 接口数据与兼容性

不改API路径、payload字段、数据库、鉴权、计费、模型路由、依赖或应用VERSION；前端显隐不应删除票据配置。

## 验证记录

| 验证 | 结果与范围 |
| --- | --- |
| 定向测试 | 首轮创建/工作台三文件74项通过；手机宽度修正后追加`OAuthAuthorizationFlow.spec.ts`，四文件77项全部通过，6.03秒；同一DOM实例、返回草稿、Session/PAT提交356、无效票据留在第一步；API Key/Agent Identity/Qoder等既有用例通过 |
| 静态/构建 | 全量ESLint通过，最终修改文件再次ESLint通过；vue-tsc/Vite通过，最初37.89秒、手机宽度修正后38.26秒，既有大chunk提示保留 |
| 浏览器 | 合成API＋真实构建，1440/390×深浅四组通过，深色减少动画；修改长度356和超时180后前进隐藏、返回保留、填1不前进、修正后可前进；pageerror为空；截图发现手机裁切后修正并重新执行四组，额外断言授权textarea完全位于弹窗内，最终手机深色截图视觉复核通过 |
| 边界 | 未调用真实授权/模型接口，未访问生产账号；本次纯前端局部修改，不重复后端全量或存储测试，不宣称全项目永远无bug |
| 文档/工作树 | 同步当前接口/操作手册和版本索引；用户原比较文档77增2删未触碰、不提交；git diff --check通过 |

前进新增异步校验后，既有测试须等待模板/步骤完成再点击导入；统一测试提交辅助函数等待Promise。新增往返用例比较真实DOM节点验证未卸载，避免测试工具每次生成的vm包装对象造成假失败。

## 发布部署与回退

代码`33116c488`和最终修正`e4d1ae0cb`均已推送main；镜像构建/隔离启动通过，固定版本0.1.278-bh.059、latest、bauhaus按顺序发布，远端三个清单一致。没有替换线上容器；可回退bh.058镜像恢复旧界面，无迁移。

## 风险与后续同步保留点

后续不能将工作台移入会被step切换卸载的分支；不得为隐藏界面删掉创建提交。前端回退会恢复重复显示，没有数据迁移。

## 追加证据与勘误

2026-09-20发布证据追加：

- 最终源码`e4d1ae0cb72eae3a068268c681923d07932d37cc`，包含步骤显隐及手机授权内容收缩；固定标签发布前两次HEAD确认不存在，未覆盖旧产物。
- `linux/amd64`，版本0.1.278-bh.059，构建提交e4d1ae0cb，时间2026-09-20T13:15:18Z；镜像内vue-tsc/Vite（36.98秒）及Go embed构建通过。
- 隔离启动无外网、无生产卷、临时目录/只读根文件系统；版本、setup/status、首页和内嵌JS/CSS均PASS。
- 三标签`ghcr.io/sourceonece/byteseek:0.1.278-bh.059`、`:latest`、`:bauhaus`均匿名校验成功，清单digest与内容SHA256均为`sha256:232e212a420295d20ad5777916f3b1acbe03dffc01dc1c6240c28568f0b581b9`；config均为`sha256:71bdf0abc5ce03744827a00880e248806b6d35ac21c334f2f763f22407cdb0e1`。
- GitHub工作流查询没有该提交运行记录，本地测试不能冒称远端CI通过；专用5201预览已停止。此次证据补录不重打镜像，未部署。
