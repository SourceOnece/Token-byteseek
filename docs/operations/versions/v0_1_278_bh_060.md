# v0.1.278-bh.060：票据长度输入数值重点色

## 章节导航

- [需求与范围](#需求与范围)、[功能变动](#功能变动)、[开发逻辑与依据](#开发逻辑与依据)
- [验证记录](#验证记录)、[发布部署与回退](#发布部署与回退)

## 元数据

- 日期：2026-09-20 Asia/Taipei；类型：局部视觉调整；实现验证完成，GHCR三个同digest标签已发布，未部署。
- fork起点`6ea20c50d`，前序[bh.059](v0_1_278_bh_059.md)。不修改应用VERSION或创建Git tag。
- 基线保持TokenRouter v0.1.278 `0eba20d2ff7e2758e4e0f53589279f8b85c33a7a`、sub2api v0.2.7 `aea725f2ea644d5592d0bbb1d63b607efa7e200a`（历史核对main `1a9d49e16f7a22c432b428fce4af8d731f1fa364`）；无上游咨询或同步。

## 需求与范围

用户要求工作台的“合格长度（字节）”输入数值加粗绿色、“异常长度信号（0关闭）”加粗红色。覆盖所有复用工作台的新增、编辑、导入模板、批量入口，只改显示，不改值和规则。

## 功能变动

| 变更前 | 变更后 |
| --- | --- |
| 两个输入值与其他数字均为默认文字色、font-bold | 合格值绿色font-extrabold，异常信号红色font-extrabold；其他字段保持原样 |

## 开发逻辑与依据

在CodexTicketAccountSettings原数字输入框按field.key追加Tailwind语义色和字重：target_length浅emerald-700/深emerald-400，degraded_signal_length浅bh-red/深red-400。批量原禁用、opacity-50保持并叠加；0只表示关闭长度信号，红色是字段语义而非账号检测结果。仍用原v-model/校验/patch/统一保存。

关键文件：`frontend/src/components/admin/account/CodexTicketAccountSettings.vue`；设计契约及操作手册补充字段颜色口径。

## 上游同步与保留差异

无上游变化，保留bh.059工作台仅第一步显示与草稿保留；包豪斯语义色不得同步回退。

## 包豪斯适配

复用现有绿/红主题配色、原输入框边框/焦点和字重；不增加底色、卡片或装饰。新增/编辑/模板/批量共用组件，禁用虚化和深色语义保持。

## 接口数据与兼容性

只有输入框class变化，无API、数据、鉴权、计费、调度、采集判定、迁移或依赖变化；不修改真实账号。

## 验证记录

| 检查 | 结果和范围 |
| --- | --- |
| 静态与构建 | 修改组件ESLint通过；vue-tsc/Vite通过，Vite38.81秒，保留原chunk大小提示 |
| 原工作台回归 | 既有25项测试全部通过，2.83秒；不新增仅映射CSS实现的单元测试 |
| 浏览器 | 合成API＋真实构建，1440/390宽×浅/深四组通过，深色减少动画；编辑/批量/模板两字段计算颜色分别为浅rgb(4,120,87)/rgb(225,37,27)、深rgb(52,211,153)/rgb(248,113,113)，字重800；未勾选disabled及opacity0.5、勾选后opacity1、焦点可达均通过 |
| 兼容与视觉 | 原模型白名单/映射保存payload不变，pageerror为空，无页面横向溢出；手机深色截图视觉核对。首轮浏览器读取尚在150ms过渡的opacity，改为等待既有动画结束后重跑四组通过，未修改业务或动效 |
| 文档与边界 | 72个相关目录链接存在、git diff --check通过；用户比较文档77增2删保留不提交；未连接生产账号/模型，纯CSS未重复后端测试 |

## 发布部署与回退

代码`638418c50`已推送main，镜像构建及隔离启动通过；因bh.059已发布，本次使用新的bh.060固定标签，随后同步latest和bauhaus，远端三个清单一致。未替换线上容器。

## 风险与后续同步保留点

颜色仅表达字段用途，异常长度0红色不代表账号降智；不能用显示色改变业务判断。回滚bh.059恢复默认文字色，无数据恢复动作。

## 追加证据与勘误

2026-09-20发布证据追加：

- 源码提交`638418c50b9cbfcdbe3209824998c662e4f29806`，`style(tickets): emphasize eligible and degradation length inputs`；固定标签构建前及推送前HEAD均确认不存在。
- 平台linux/amd64，版本0.1.278-bh.060，内嵌提交638418c50，构建时间2026-09-20T13:24:43Z；镜像内vue-tsc/Vite（37.39秒）、Go embed通过。
- 隔离启动无外网、无生产卷、只读根文件系统和临时目录；版本、setup/status、首页及内嵌JS/CSS均PASS。
- 三标签`ghcr.io/sourceonece/byteseek:0.1.278-bh.060`、`:latest`、`:bauhaus`均匿名读取校验通过，清单digest与内容SHA256为`sha256:7c2bc715cd8a6646848fde8973f919b0186f1a92c2448aac4220d9509efa86ee`；config均为`sha256:d358eb42bb94fb7cb4e14fff7d82ece978a331fce3a1fff18515102d3b803fb9`。
- 专用5202预览已停止，用户原比较文档保留；GitHub工作流查询未返回本提交运行记录，未冒称远端CI通过。补录不递归编号、不重新构建，未部署。
