# v0.1.278-bh.048：号池调度、题目检测与票据配置通俗手册

本版仅新增代码核对后的管理员操作说明，不改变应用功能。应用仍为bh.047，不发布bh.048镜像。正文见[Codex号池操作手册](../codex_pool_operator_guide.md)。

## 元数据

- 日期：2026-09-19 Asia/Taipei；类型：纯文档，完成。
- 起点：`a25e288b8`；应用源码`54bd681a6c649721090e0465dfc811059d2b802f`，前序[bh.047](v0_1_278_bh_047.md)。
- TokenRouter v0.1.278 / `0eba20d2ff7e2758e4e0f53589279f8b85c33a7a`，sub2api v0.2.6 / `49a39b6dc1abed30fd227611e8af1108bc427610`均不前移；不作新上游咨询或移植。

## 需求与范围

用户要求按当前代码，通俗且逐项解释调度、降智题目检测、定时计划、采集票据、双链路、代理、守护的配置和相互影响，并提供Markdown文件。仅修改文档，不借机修正业务或UI。

## 功能变动

应用无变化；新增运维手册、规范目录入口及版本留档。现有散落的接口/版本说明仍保留，手册提供面向管理员的阅读顺序、配置表、流程、示例及排查入口。

## 开发逻辑与依据

逐项对照前端实际控件/默认草稿和服务端有效默认、校验范围、采集/题目测试/守护写调度的代码路径。重点区分题目结果与票据、账号总调度与模型门控、题目120秒与采集25秒、原生WS与可选桥接、自动检查与定时计划。明确题目测试目前不注入缓存STATE，守护默认observe仍联动长度调度，0次数不绕过上游退避，缺票不直接改账号总开关。

关键依据：`codex_quality_service.go`、`account_test_service.go`、质量计划service/repository、`codex_ticket_rules/settings/account_settings/manual/probe/verify_probe/scheduling/watchdog`、对应Vue表单和中英文文案；源文件定位汇总于手册末尾。

## 上游同步与保留差异

不涉及同步，沿bh.047三方及STATE Kit采用边界；不重新引入原项目固定套餐长度或直连分支。

## 包豪斯适配

不改UI。说明延续短标签、分区、关系表/流程图和文字含义；满血绿、降智红、失败黄等现有语义如实说明，不用颜色代替结论。

## 接口数据与兼容性

无API/鉴权/调度/计费/迁移/依赖/VERSION变更。只记录合成示例，不存真实账号、取号密钥、代理密码或票据。

## 验证记录

代码及表单只读对照；手册774行，Markdown链接、目录成员、差异范围和空白检查通过。本版不重跑应用套件、不调用真实上游、不查生产设置；明确默认值不等于用户已部署值。

## 发布部署与回退

纯文档不打包、不创建应用标签、不移动latest/bauhaus、不部署。最新应用仍为bh.047，远端清单`sha256:6307351b402715f4d2b1a6c5e767bef389f31879c98fb4585ec9b65f646eb1dc`。文档回退只移除本手册及目录引用，不回滚应用或改数据。

## 风险与后续同步保留点

后续业务变化需同步手册，不能拿旧版说明覆盖新代码；历史继承值以账号编辑加载结果为准。发现旧UI提示“采集不改调度”与现行逻辑不同，仅在手册标明，未经授权不改代码。

## 追加证据与勘误

纯文档版本不打包、不创建应用标签、不移动latest/bauhaus、不部署；后续业务变化需同时更新手册、当前契约和版本记录。

2026-09-19 Asia/Taipei勘误：后续审查发现本版记录漏加到versions/index.md，原“目录成员检查通过”未覆盖该遗漏。由[bh.049](v0_1_278_bh_049.md)补齐索引，并随代码修正采集旧UI说明和更新操作手册；本版当时为纯文档、未改应用的历史事实不变。
