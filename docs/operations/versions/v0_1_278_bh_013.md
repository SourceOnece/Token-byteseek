# v0.1.278-bh.013：回退 bh.002 应用逻辑并保留台北时区

按用户明确授权撤回 bh.003–bh.012 的 Metadata 修复系列，应用恢复 bh.002，唯一保留其后新增的 Asia/Taipei 设置选项。开发历史和包豪斯约束保留，发布后不自动替换线上容器。

## 章节导航

- [元数据](#元数据)
- [需求与范围](#需求与范围)
- [功能变动](#功能变动)
- [开发逻辑与依据](#开发逻辑与依据)
- [上游同步与保留差异](#上游同步与保留差异)
- [包豪斯适配](#包豪斯适配)
- [接口数据与兼容性](#接口数据与兼容性)
- [验证记录](#验证记录)
- [发布部署与回退](#发布部署与回退)
- [风险与后续同步保留点](#风险与后续同步保留点)
- [追加证据与勘误](#追加证据与勘误)

## 元数据

| 字段 | 内容 |
| --- | --- |
| 文档 ID / 日期（含时区） | v0.1.278-bh.013 / 2026-09-07 Asia/Taipei |
| 类型 / 交付状态 | 用户授权功能回退；代码恢复与本地验证完成，待构建/推送，未部署 |
| 前序记录 | [bh.012](v0_1_278_bh_012.md)；恢复目标 [bh.002](v0_1_278_bh_002.md)，保留 [bh.009](v0_1_278_bh_009.md) 的台北选项 |
| 应用版本 / upstream SHA | 0.1.278 / 1910b44ac；不因回退编号修改 VERSION 或 Git tag |
| fork 起点 / 目标 | ee0f3700e1545371ae9c055b1f906e7c6ff0fa5b → 应用基线 f9f51f1285e7af0b7fa0a5a7b22a802436e4cda1；实现提交由本文件历史定位 |

## 需求与范围

用户要求“回退到002版本，也就是取消metadata修复，然后Asia/Taipei保留，然后重新打包镜像为最新版”。这是代码功能回退及双标签发布，不是仅关闭开关，不允许清空用户数据或重写远端历史。本轮保持当前 main，用新的回退提交保留历史；不替换线上容器。

## 功能变动

| 来源与功能 | 回退前 | 回退后 | 范围 |
| --- | --- | --- | --- |
| Metadata 修复 | 存在单账号/批量开关、快照、连接私有状态、父子引用补齐等 | 删除该 fork 功能及专属测试，恢复 bh.002 原转发和连接处理 | HTTP/透传/Chat/Messages/WS、账号表单 |
| Asia/Taipei | bh.009 增加目标时区预设 | 原样保留 SettingsView 和对应测试 | 用户提示词替换，默认规则不变 |
| 指纹与原业务 | bh.002 已有收敛、隔离、路由、计费及错误处理 | 恢复/保留原基线，不把所有名为 metadata 的上游协议字段删除 | 应用整体 |
| 文档/历史 | bh.003–012 的历史事实 | 不删除历史，新记录说明撤回和恢复边界 | 工程文档 |

## 开发逻辑与依据

bh.002 是 f9f51f128 的纯治理提交，应用与当时 bh.001 一致。读取 Git 差异后逐文件恢复 backend/frontend 中所有后续变化，排除仅承载台北预设及其回归的 SettingsView.vue 与 SettingsView.spec.ts。验证目标为 backend 整树与 bh.002 一致，frontend 相对 bh.002 仅保留 ae59e1f47 的两个台北文件差异。不用 reset/force push，不清理历史提交。

删除 Codex Metadata 完整性修复的创建/编辑/批量 UI、i18n、后端快照/关联/会话辅助与各转发钩子，同时恢复被修改的公共 WS 支持函数、连接兼容条件及原测试。原账号配置中的未知 extra 键不做数据库清除；回退代码没有消费者，残留值不再启用修复。原平台 metadata、指纹配置和已保存时区仍按基线处理。

| 文件/符号 | 改动职责与关联证据 |
| --- | --- |
| backend/internal/service/openai_gateway_*.go、openai_ws_*.go | 按 bh.002 恢复，不保留后续 Metadata 分支 |
| frontend/src/components/account 三个表单及中英文文案 | 去除完整性修复选项，其它字段精确回到基线 |
| [SettingsView](../../../frontend/src/views/admin/SettingsView.vue) 与测试 | 保留台北预设，与 bh.009 相同 |
| [OpenAI 契约](../../interfaces/openai_upstream.md) | 当前状态标明移除；历史文档继续可追溯 |

## 上游同步与保留差异

不拉取合并 TokenRouter 上游；应用基线仅为 bh.002 加台北预设。Git 历史、版本约束与全站包豪斯设计契约不回退、不删除。

## 包豪斯适配

只移除后来新增的 Metadata 控件，恢复基线包豪斯账号表单；保留原 Toggle/Select、直角、硬阴影、按压和主题资源。台北仍使用自研 Select，不改页面布局。视觉/组件验证证据在下方填写，不把精确源码恢复写成全站浏览器已测。

## 接口数据与兼容性

无 API 路径、数据库迁移、依赖或 VERSION 变更。extra.codex_metadata_repair_enabled 不再被运行时读取；不需要修改数据库或逐账号关闭。若未来重新恢复该功能，残留 true 可能重新生效，届时必须单独审核。原指纹 session/full 行为回到 bh.002；更新容器后应重连 Codex 会话，不保证沿用新版生成编号的旧链。

## 验证记录

| 命令/操作 | 目标与环境 | 结果与覆盖 | 限制 |
| --- | --- | --- | --- |
| 改前 Git 范围检查 | ee0f3700e，工作区干净 | 已核实 bh.002、台北保留文件与后续新增列表 | 不访问用户数据库 |
| 精确恢复与残留检查 | git diff f9f51f128 -- backend/frontend；搜索原功能符号 | 后端整树与 bh.002 完全一致；前端只差 SettingsView 的 2 行台北预设及 39 行原有回归测试，与 bh.009 相同。业务源码/前端产物无 codex_metadata_repair_enabled、CodexMetadataRepair 等残留 | 历史文档保留该名称属于有意留档 |
| 前端组件 | 6 个 Vitest 文件：Create/Edit/BulkEditAccountModal、SettingsView、GroupClientProtocolSelector、Select | 195 项通过，18.33s，含台北两种动态规则的默认不变/选择/保存/回显 | SettingsView 的 router-link stub 警告非失败 |
| 前端 lint、类型与构建 | pnpm lint:check、pnpm build（vue-tsc + Vite） | 全通过，Vite 40.35s | 既有分块体积、混合导入警告未改；不借回退重构前端 |
| 后端原链路回归 | 禁网 Go 1.27.0，service/handler/routes/middleware/domain；Test.*(Codex/OpenAI/ForwardAsAnthropic/ForwardAsChatCompletions/CapacityShed/Fingerprint/Gateway.*Protocol/GroupClientProtocol) | 首轮 service 被 bh.002 原有测试桩中断；单独复现并仅 skip 该测试后，service 63.637s、handler 6.588s、routes 0.036s、middleware 0.022s、domain 0.005s 全通过 | TestOpenAIGatewayService_OAuthPassthrough_BrowserUAUsesConfiguredCodexUA 的 GetMultiple 桩显式 panic；精确恢复也恢复了此旧测试缺陷，非生产实现，未修改它或写成全包全绿 |
| 本机浏览器 | 1440×1000/390×844、中文浅色/英文深色、普通及 reduced-motion，全部 API 模拟 | 批量弹窗无 Metadata 控件，原状态编辑和键盘焦点可用；台北两个选择框可搜索/键盘打开，直角与 4px 硬阴影保留，无全页横向溢出/脚本错误；人工查看手机深色弹窗和桌面台北菜单截图 | 创建/编辑依组件测试与精确源码比对，未逐个浏览器重测；非全站视觉审计，不提交生产 API |
| 文档/空白 | git diff --check、变更链接与显式锚点检查 | 空白检查通过，链接随交付复核 | 无真实 OAuth 或生产 DB 验证 |

## 发布部署与回退

构建新的 linux/amd64 应用镜像，标识 bh.013；应用 VERSION 仍为 0.1.278，推送 ghcr.io/sourceonece/byteseek:bauhaus 与 latest 同一候选并核对 digest。此记录提交时尚未构建/推送/部署，完成后追加证据，不覆盖历史状态。

回退前 bh.012 不可变引用：ghcr.io/sourceonece/byteseek@sha256:bcd18cef0d3ed5e928c5b05ad9f176d602538cbc6e448735aa14895ef78a919f。无数据库变更，可通过应用镜像恢复；不得未经授权删除已保存配置。

## 风险与后续同步保留点

本版是用户选择恢复旧逻辑，不声称原 overloaded/断流得到解决。不能在未来同步时误把撤销的 Metadata 补丁自动加回来；Asia/Taipei 与包豪斯契约必须继续保留。发布不等于已部署。

## 追加证据与勘误

无。

### 2026-09-07（Asia/Taipei）回退产物发布完成

- 实现提交 c9d97c24e8666e871d1d4cce79a6874e05f838c2 已正常快进推送 GitHub main。提交后复核 backend 与 f9f51f128 的 Git diff 为空，frontend 只保留台北的两个文件；原 Metadata 修复功能符号在业务源码及新静态产物中均无残留。历史代码可通过 Git 恢复。
- 使用原 Dockerfile 重建 linux/amd64，VERSION=0.1.278、COMMIT=c9d97c24e8666e871d1d4cce79a6874e05f838c2、DATE=2026-09-07T03:20:00Z；OCI version=0.1.278-bh.013，source 指向本 fork。前端在 Docker 内重新类型检查/构建通过，Go release embed 构建通过；未修改依赖、Dockerfile 或版本文件。
- 本地候选 byteseek:bauhaus-candidate-bh013，image/config ID=sha256:783f0a9ec17a5674832689cf66beea619cbb533070c05d15c90507db80ffa076。禁外网、无生产挂载启动检查通过：版本/提交、安装态 /setup/status、内嵌 /home、Bauhaus CSS、台北预设、原默认时区规则和原指纹字段保留；AccountsView bundle 无 codex_metadata_repair_enabled/codexMetadataRepair。pg_dump/psql 18.6 可执行，未连接生产 DB。
- ghcr.io/sourceonece/byteseek:bauhaus 与 latest 均已推送成功。分别读取远端 verbose manifest，digest、config 和平台完全相同；空 Docker 登录配置匿名读取 bauhaus manifest 成功。
- 不可变镜像引用：ghcr.io/sourceonece/byteseek@sha256:0939ecb0153c2b475570fe73db780a1f4b2f2f795c5f29dc682d9ea7ebb33b07。
- 本节仅补录发布证据，不循环构建应用镜像；线上容器未替换、账号/数据库未修改。新镜像部署后才取消运行中的修复，建议重新连接原 Codex WS。
