# v0.1.278-bh.014：先恢复 bh.002 再加入台北选项及清理旧文档

按用户进一步明确的要求执行“先退、再加”，应用基线为 bh.002，新增仅限 Asia/Taipei 预设及测试。删除当前工作树中的 bh.003–bh.013 文档，保留 bh.001/bh.002、工程约束和本次必要回退证据，不改写 Git 历史。

## 章节导航

- [元数据](#元数据)
- [需求与范围](#需求与范围)
- [功能变动](#功能变动)
- [开发逻辑与依据](#开发逻辑与依据)
- [上游同步与保留差异](#上游同步与保留差异)
- [包豪斯适配](#包豪斯适配)
- [接口数据与兼容性](#接口数据与兼容性)
- [验证记录](#验证记录)
- [发布部署与回退](#发布部署与回退)
- [风险与后续同步保留点](#风险与后续同步保留点)
- [追加证据与勘误](#追加证据与勘误)

## 元数据

| 字段 | 内容 |
| --- | --- |
| 文档 ID / 日期（含时区） | v0.1.278-bh.014 / 2026-09-07 Asia/Taipei |
| 类型 / 交付状态 | 顺序重建核验与旧文档清理完成；应用净差异为零，不重打镜像 |
| 前序依据 | [bh.002](v0_1_278_bh_002.md)；应用来源 [bh.001](v0_1_278_bh_001.md)；删除前工作树提交 3add1ae0d |
| 应用版本 / upstream SHA | 0.1.278 / 1910b44ac，不修改 |
| fork 起点 / 基线 | 3add1ae0d → f9f51f1285e7af0b7fa0a5a7b22a802436e4cda1 后只加台北；本次提交由此文件 Git 历史定位 |

## 需求与范围

用户说明必须先完整恢复 bh.002，再在它的基础上增加 Asia/Taipei，并删除其他版本及文档，避免后续误带 Metadata 代码。此次以用户最新指令为明确的旧版本文档删除授权，覆盖通常保留旧文档文件的默认要求；保留必要基线和本次回退证据，不推断为删除远端镜像、Git 提交或生产数据的授权。

## 功能变动

| 项目 | 执行前 | 执行后 |
| --- | --- | --- |
| 应用代码 | 已是 bh.002 + 台北，但上轮按保留台北文件方式恢复 | 本次先临时移除台北并核验应用完整等于 bh.002，再单独添加；最终与已发布应用相同 |
| 当前 OpenAI 工程说明 | 含已撤回 Metadata 的后续说明 | 恢复 bh.002 原文，不保留该功能的当前说明 |
| 版本文档 | 工作树保留 bh.003–bh.013 | 删除这 11 份记录及目录项，旧内容仅留 Git 历史；保留 001/002 与本记录 |
| 线上/镜像 | 已发布的最新镜像是 bh.002 + 台北 | 无应用变化，不重打相同镜像、不改变线上容器 |

## 开发逻辑与依据

1. 开始时工作树干净。全仓库相对 f9f51f128 的差异只有两个台北文件、OpenAI 后续说明与版本目录/记录，没有其它应用代码。
2. 将 SettingsView.vue、SettingsView.spec.ts 和 OpenAI 工程说明先恢复为 bh.002。此时执行 git diff f9f51f128 --exit-code -- backend frontend 返回 0，证明台北新增前完整应用树与 bh.002 相同。
3. 在这一基线上仅重新添加 Asia/Taipei 的 2 行预设与 39 行测试，保持默认 Asia/Tokyo、已有规则和保存接口不变。
4. 使用 apply_patch 删除明确列出的 v0_1_278_bh_003.md 至 v0_1_278_bh_013.md，移除目录中的旧入口。全仓库搜索清理失效引用，当前专题恢复基线，后续查阅不会误用 Metadata 修复文档。
5. 采用新提交、正常推送，不 reset/强推；文档删除可通过 3add1ae0d 等历史提交恢复。没有删除 Docker/数据库或客户内容。

| 文件 | 职责 |
| --- | --- |
| [SettingsView](../../../frontend/src/views/admin/SettingsView.vue) 及其测试 | 本次先恢复再单独加入台北；最终仍为唯一前端功能增量 |
| [OpenAI 上游](../../interfaces/openai_upstream.md) | 完全恢复 bh.002 文本 |
| [版本目录](index.md) | 仅保留基线与当前清理证据 |

## 上游同步与保留差异

不 fetch/merge 上游、不修改分支。后端与 bh.002 全等，前端仅台北例外；AGENTS、包豪斯契约、其余配置/依赖/迁移均保持 bh.002 原内容。文档编号递增仅供历史追踪，不意味着重新引入被删除的功能。

## 包豪斯适配

无最终 UI 差异，台北仍使用 bh.002 自研 Select，原有主题、直角、硬阴影和按压行为不动。本次按代码哈希验证与此前已验证镜像的输入相同，不重复宣称完成全站视觉检查。

## 接口数据与兼容性

无新 API、鉴权、计费、路由、数据结构或迁移。后端回到 bh.002 原始 metadata/指纹处理；取消的是后续 fork 的完整性修复，不删除上游原协议字段。数据库残留未知 extra 键不清除，当前代码无该功能消费者。

## 验证记录

| 检查 | 结果 | 限制 |
| --- | --- | --- |
| 先退基线 | backend/frontend 相对 f9f51f128 差异为空，返回 0 | 只比对受版本控制文件，不清除用户未跟踪内容 |
| 再加台北与最终边界 | backend 与 bh.002 全等；frontend 仅 SettingsView 2 行、对应测试 39 行。相对已发布源码 c9d97c24e 的 backend/frontend/Dockerfile/.dockerignore/deploy 全无差异 | 未跟踪文件不据此清除 |
| 台北回归 | Vitest 精确重跑 offers Asia/Taipei 用例通过（458ms），验证两种规则默认/选择/保存/回显，其余 57 项未重跑 | 既有 router-link stub 警告非失败；相同应用输入的完整验证见历史提交 c9d97c24e |
| 旧文档及引用清理 | 已删除 bh.003–bh.013 共 11 份，目录仅 001/002/014 和模板；本次变更文档链接、空白检查通过，无指向已删除文档的链接 | Git 历史仍保留，不删除历史镜像 |
| 功能残留 | backend/frontend 源码搜索无 codex_metadata_repair、CodexMetadataRepair、codexMetadataRepair | 不删除上游原生 metadata 协议字段 |

## 发布部署与回退

当前 latest/bauhaus 的已发布应用源码 c9d97c24e8666e871d1d4cce79a6874e05f838c2 已是 bh.002 + 台北。最终应用树如与该提交全等，则只提交文档清理，不重建或重推同一应用镜像，按版本留档规则避免无意义重打。先前发布不可变引用为 ghcr.io/sourceonece/byteseek@sha256:0939ecb0153c2b475570fe73db780a1f4b2f2f795c5f29dc682d9ea7ebb33b07，linux/amd64；本次是否重新查询远端另记。没有部署或生产配置操作。

## 风险与后续同步保留点

用户授权删除的文档仅从当前树移除，Git 日志仍可追溯。不能把“清理文档”解释为代码安全性的证明，真正边界由与 bh.002 的完整树差异证明。后续应继续基于 bh.002 + 台北开发，不自动恢复被撤回补丁；任何新变动仍遵循包豪斯与版本门禁。

## 追加证据与勘误

无。

2026-09-07（Asia/Taipei）最终核对：远端 latest manifest 仍为 sha256:0939ecb0153c2b475570fe73db780a1f4b2f2f795c5f29dc682d9ea7ebb33b07，config=sha256:783f0a9ec17a5674832689cf66beea619cbb533070c05d15c90507db80ffa076，linux/amd64。与本次重新“先退再加”后的应用树相同，因此仅推送文档清理提交，不构建、不推送新镜像、不部署。当前文档只保留基础版本和本清理记录；后续编号不复用已删除的编号。
